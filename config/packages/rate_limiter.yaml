framework:
    rate_limiter:
        password_reset:
            policy: 'fixed_window'
            limit: 3
            interval: '15 minutes'
        newsletter_subscribe:
            policy: 'fixed_window'
            limit: 3
            interval: '15 minutes'
